@model IEnumerable<CV_siten.Models.Projekt>

@{
    ViewData["Title"] = "Sökresultat";
    var personer = ViewBag.personResult as List<CV_siten.Models.Person>;

}

<h2 class="text-center">Sökresultat för "@ViewBag.SearchQuery"</h2>

@* <div class="row">
    <div class="col-md-6">
        <h3>Matchande Projekt</h3>
        @if (Model.Any())
        {
            @foreach (var p in Model)
            {
                <p><a asp-controller="Projekt" asp-action="Details" asp-route-id="@p.Id">@p.Projektnamn</a></p>
            }
        }
        else
        {
            <p>Inga projekt hittades.</p>
        }
    </div> *@

    <div class="text-center">
        <h3>Matchande Personer</h3>
        @if (personer != null && personer.Any())
        {
            @foreach (var pers in personer)
            {
                <button>@pers.Fornamn @pers.Efternamn (@pers.Yrkestitel)</button>
            }
        }
        else
        {
            <p>Inga personer hittades.</p>
        }
    </div>
</div>